SERVUBER — BUILD PROGRESS
=========================
Phase 1 (MVP) per askr-build-prompt-v2.md. Behaviour and visual language now
from PRD/servuber-prd.html and PRD/servuber-mockup.html (v1.1).

See docs/feature-status.md for the current feature list. Short version: v1.1
added six features, ALL Phase 2, and Phase 1 scope is unchanged. Tokens are
unchanged. Mockup screens renumbered — a new speed screen at 03 shifts the
old 03-09 to 04-10, so older "mockup screen N" references are off by one.

Brand: Servuber — "Service at your choice of price" (src/lib/brand.ts).
The Postgres schema stays `askr`: it is an infra name, not the brand.
Repo: https://github.com/smohap/Askr (branch main).


DONE
----
Step 1 — scaffold, tokens, schema, RLS, seed
  * Next.js 16.3 App Router + TypeScript + Tailwind v4. Turbopack is default.
  * src/styles/tokens.css holds the mockup :root verbatim and is the ONLY file
    allowed a colour literal; globals.css maps it into Tailwind's @theme.
  * 6 migrations: schema, RLS, storage (5 buckets), grants, matching.
  * seed.sql: 18 categories, 1 buyer, 1 admin, 3 verified + 1 pending provider.
  * src/lib/escrow/machine.ts — the state machine, pure.

Step 2 — auth and role routing
  * src/proxy.ts (Next 16's middleware.ts) refreshes the session and bounces
    guests off protected prefixes. No role check there — needs a DB read.
  * src/lib/auth.ts — getViewer/requireViewer/requireRole. Uses getUser(),
    not getSession(): getSession trusts the cookie, getUser revalidates.
  * Login (password + magic link), signup with buyer/provider choice,
    /auth/callback, sign out. Vague failures on purpose — see DECISIONS 7.
  * src/components/ui/* — Button, Panel, Field, Avatar, LiveDot, Badge, TopBar.
  * Home screen (mockup 01) serving guest and buyer.

Step 3 — request creation
  * src/lib/validation/request.ts — Zod, shared client/server. CATEGORY_FIELDS
    is data: adding a category is a table entry, not a branch.
  * /requests/new (mockup 02), /requests list with Live/Drafts/Closed.
  * Media rides in the same FormData and uploads after insert, so the request
    id is available as the storage folder the policies authorise on.

Step 4 — matching and broadcast (UI done; notifications are DB rows only)
  * askr.match_providers() — verified + category + BOTH radii satisfied.
  * publishRequest() runs as service role because request_broadcasts has no
    insert policy.
  * Broadcasting screen with the radar sweep (mockup 03).

Step 5 — offers (buyer half done)
  * src/lib/offers/best-match.ts + tests — the documented Best match rule.
  * OfferStack with sort by price/rating/ETA/distance, live via Realtime,
    visible expiry countdown (mockup 04).

Step 7 — escrow, Stripe end of it
  * src/lib/stripe/server.ts — the client, created lazily. A top-level client
    makes STRIPE_SECRET_KEY a build-time requirement, and `next build` imports
    every route module to collect page data.
  * src/lib/stripe/escrow.ts — hosted Checkout, transfer, refund, Connect
    onboarding. src/lib/escrow/settle.ts pairs each money move with its
    transition; src/lib/escrow/actions.ts is every buyer/provider affordance.
  * /api/stripe/webhook — signature-verified, idempotent on event id.
  * /orders/[id]/pay (mockup 08), /orders/[id] with the timeline (mockup 09),
    /provider/jobs + /provider/jobs/[id], /provider/payouts.
  * The dangling /orders/[id]/pay route is now real.

Step 8 — ratings and reviews
  * /orders/[id]/review (mockup 10) — star picker, one review per order.
  * /provider/reviews — the provider's right of reply, reply columns only.
  * Reviews render on the offer page, where the buyer is deciding.

Step 10 — admin console
  * (admin) route group: /admin overview, /admin/verification, /admin/disputes
    and /admin/disputes/[id].
  * Verification: per-document approve/reject and a separate provider decision,
    both refusing a rejection with no reason. Documents open through signed
    URLs, one hour.
  * Disputes: the resolve form writes one reason and applies it whichever way
    the admin decides, then releases or refunds.
  * Overview reports settled GMV, commission, money currently in escrow, orders
    by state and what is waiting on an admin.

Step 11 — tests
  * src/lib/escrow/transition.test.ts — transition() against a stubbed client:
    that it validates before writing, passes the from-state it validated
    against, and turns a lost race into a TransitionError. 46 unit tests pass.
  * e2e/buyer-journey.spec.ts — post, offer, accept, pay, start, deliver,
    confirm, review. e2e/admin.spec.ts — verify a provider, then refund a
    disputed order.
  * playwright.config.ts. NOTHING IS STUBBED in e2e: it needs a live database,
    Stripe test keys and `stripe listen` forwarding to the webhook, because the
    order does not leave pending_payment without it.

46 unit tests passing (`npm test`). `npx next build` clean.


NEXT
----
Phase 1 is feature-complete. Nothing is left in the build order.

Before this can be trusted, in priority order:

1. Run the migrations. Still true that no SQL in this repo has ever executed
   (see ENVIRONMENT NOTE). Everything above is written against a schema no
   Postgres has accepted yet.
2. Run the e2e suite. It has never run either — the same reason, plus no
   Stripe keys on this machine.
3. database.types.ts, once there is a live DB to generate from. Joined rows and
   RPC results are hand-typed with `as unknown as` until then, so a schema
   change will not break the build where it should.

Two lint errors predate this session and remain: react-hooks/purity on the
Date.now() in the offer page, and react-hooks/set-state-in-effect in
chat-thread.tsx. Both are in shared components on the accept path; neither was
touched here because fixing them is a behaviour change, not a step.

Known gap worth naming: a provider who has not finished Connect onboarding
blocks release — releaseOrder() throws with their business name in it. The
right fix is to stop them offering at all until payouts are enabled, which is
a change to the offer form, not to escrow.


DECISIONS THAT WOULD NOT BE OBVIOUS
-----------------------------------
1. Escrow is enforced in two layers, deliberately.
   - src/lib/escrow/machine.ts is PURE: the legal-move table, actor
     entitlement, required reasons. No DB, so it is exhaustively unit-testable
     with no Docker. machine.test.ts hard-codes the eight documented moves
     separately so the table cannot drift silently.
   - askr.apply_order_transition() is the atomic tail: SELECT ... FOR UPDATE,
     re-check the from-state, UPDATE orders, INSERT order_events, one
     transaction. The orders_state_guard trigger raises on any other UPDATE,
     including from the service role.
   - order_events has a BEFORE UPDATE OR DELETE trigger that always raises.
     Append-only is enforced at the table, not RLS, because RLS does not apply
     to the service role.

2. Money model. Buyer pays the quote plus a 5% platform fee
   (COMMISSION_BPS = 500). On release the platform keeps that fee as commission
   and the provider receives exactly what they quoted, so
   commission_cents === platform_fee_cents. Reproduces the mockup's
   $170.00 / $8.50 / $178.50.

3. request_broadcasts is both the notification log and the RLS predicate. A
   provider can read a request because a row links them to it — which is why
   private requests need no second mechanism, and why only the service role may
   write that table.

4. No counter-offers (Phase 2). The partial unique index
   offers_one_active_per_provider WHERE status='active' is what makes
   withdraw-and-re-offer the negotiation mechanism instead.

5. Admin is not self-assignable. handle_new_user() maps signup metadata to
   buyer/provider only; seed.sql promotes the one admin with a direct UPDATE.
   guard_provider_self_service() strips verification_status, rating fields and
   stripe_account_id from any non-admin update.

6. Geo is a plain haversine SQL function (askr.distance_km), not PostGIS.
   Matching needs BOTH radii to hold: the provider will travel that far AND the
   buyer will look that far. One-sided matching fails in both directions.
   No geocoder in Phase 1 — buyers pick from src/lib/nz-locations.ts, which
   carries the coordinates. Swapping in a geocoder changes only that file.

7. Auth failures are deliberately vague, and the magic-link form returns the
   same notice whether or not the address exists. Distinguishing "no such
   account" from "wrong password" enumerates registered emails.

8. Escrow is separate charges and transfers, not delayed capture. The buyer's
   card is charged in full at accept time and the money sits in the platform
   balance; on release the provider's share is transferred to their connected
   account and the commission stays behind. Manual capture would have been
   simpler, but a card authorisation expires in seven days and a scheduled job
   can sit in escrow longer than that. This is why orders carries both
   stripe_transfer_id and stripe_account_id.

9. Stripe runs before the transition, never after. releaseOrder() and
   refundOrder() call Stripe first and record what it returned. Ledger-first
   can leave an order marked released with nothing transferred, and that is
   indistinguishable afterwards from a real release. Both Stripe calls carry an
   idempotency key derived from the order id, so the retry after a failed
   transition is safe.

10. Hosted Checkout, not an embedded card form. Card details never reach this
    app and there is no client Stripe dependency to keep current. The cost is a
    redirect out and back; success_url is a navigation and is never treated as
    proof of payment — only the webhook moves the order.

11. The webhook deletes its own idempotency row when the handler throws. The
    row is inserted first so a Stripe retry is dismissed as a duplicate; if the
    handler then fails, that row would dismiss the retry too. Deleting it
    before returning 500 is what makes the retry mean something.

12. Reviews and verification decisions run under the acting user's session, not
    the service role. reviews_insert_buyer already requires the order be theirs
    and released, and guard_review_reply() puts rating and body back on a
    provider's reply. Re-checking those in TypeScript would be a second copy of
    a rule that already exists in one place.

13. Best match uses a 0.15 rating band, calibrated against the mockup's own
   stack (4.9/$170, 4.8/$160, 5.0/$190, badge on the 4.9). A 0.2 band pulls the
   4.8 in and the badge moves to the cheaper offer; no band at all moves it to
   the dearest. The band is what separates "near-identical rating, take the
   cheaper" from "genuinely better rated, worth the money". A provider with no
   ratings can never be best match — they have no rating to compare.


DEPLOYMENT
----------
Target: Vercel + hosted Supabase. Tables live in schema `askr`, NOT public.

Two settings that silently break everything if missed:
  * Supabase > Project Settings > API > Exposed schemas must include `askr`.
    Without it every query returns "The schema must be one of the following".
  * The supabase-js clients pass { db: { schema: "askr" } }. Any new client
    must do the same or it queries public and finds nothing.

Custom schemas get none of the grants Supabase applies to public, which is why
20260809000050_schema.sql sets usage + default privileges and
20260809000400_grants.sql re-applies them explicitly after the tables exist.
apply_order_transition and match_providers have EXECUTE revoked from
anon/authenticated — both are service-role operations.

Realtime: OfferStack subscribes to postgres_changes on schema `askr`, table
`offers`. The publication is set up in 20260809000100_schema.sql; confirm the
four tables are listed after the first push.

Vercel env vars: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY,
SUPABASE_SERVICE_ROLE_KEY, STRIPE_SECRET_KEY,
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY, STRIPE_WEBHOOK_SECRET, NEXT_PUBLIC_SITE_URL.


ENVIRONMENT NOTE
----------------
This machine has no Docker and no Supabase CLI, so NONE of the SQL has ever
been executed. Six migrations, ~1000 lines, unverified against a live Postgres.
This is the largest untested risk in the repo. `./init.sh local` (Docker) or
`./init.sh hosted` (linked project) applies them; expect to fix errors on the
first run.

Node 26.4.0 / npm 11.17.0. npm 11 gates install scripts: if esbuild or
unrs-resolver report missing binaries, run
`npm approve-scripts esbuild unrs-resolver`.

.claude/settings.local.json sets ECC_DISABLED_HOOKS for the GateGuard
fact-forcing hook, which otherwise blocks every file write in this build.
